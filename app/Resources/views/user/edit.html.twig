{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Édition du profil</h1>

<div class="container">
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        
    {{ form_end(edit_form) }}

    <ul>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit"><i class="actionButton fa fa-trash"> Supprimer</i></input>
            {{ form_end(delete_form) }}
        </li>
    </ul>

    </div>
{% endblock %}
