{% extends 'layout.html.twig' %}
{% block title %}Accueil{% endblock %}

{% block body %} 
<h1>{{title}}</h1>
{% for product in lastProducts %}


<span>{{ product.title }}</span>
</br>
<div class="container">{{product.summary | raw }}</div>
<a class="actionLink" href="{{ path('display_course', { 'idProduct': product.idProduct }) }}"></br>En savoir plus</a>


    <div class="row">
        <div class="col-xs-12">
            <form action="/your-server-side-code" method="POST">
                <script
                        src="https://checkout.stripe.com/checkout.js" class="stripe-button"
                        data-key="pk_test_TNNrshUHsHUrIfOTaouoRwVW"
                        data-amount="4700"
                        data-name="Paiement par CB"
                        data-description="Widget"
                        data-locale="auto"
                        data-zip-code="true"
                        data-currency="eur">
                </script>
            </form>
        </div>
    </div>
{% endfor%}
{% endblock %}