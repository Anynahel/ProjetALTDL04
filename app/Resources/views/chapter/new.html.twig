{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Création d'un chapitre pour la formation n° {{ ProdName }}</h1>
<div class="container">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
    {{ form_end(form) }}
</div>
    <ul>
        <li>
            <a href="{{ path('chapter_index') }}">Retour</a>
        </li>
    </ul>
{% endblock %}
{% block javascripts %}
<script>
    CKEDITOR.replace( 'form[content]' );
</script>  
{% endblock %}
